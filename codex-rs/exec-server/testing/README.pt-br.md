# Dispositivo de fixação para servidor executivo do Windows

Este diretório contém o pequeno arquivo binário do servidor executável do Windows usado por
testes em sistemas operacionais estrangeiros. Ele vincula apenas `codex-exec-server` porque o Codex completo
O Windows Graph ainda não permite a compilação cruzada com o Bazel.
